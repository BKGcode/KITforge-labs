# <Tool Name>

Short one-line description of the problem this tool solves.

## What it is for

Use this tool when you need to:
- solve a repeated Unity workflow quickly
- avoid a common manual error
- reduce editor friction in a specific task

## Quick Start

1. Open `<menu path or window>`
2. Configure the basic options
3. Click `<main action>`
4. Review the result in `<scene / inspector / console / output>`

## Example workflows

### Example 1 — Basic use

**Problem:** You need to `<common pain point>`.

**Flow:**
1. Select `<object or asset>`
2. Open `<tool>`
3. Press `<button>`
4. The tool `<result>`

### Example 2 — Safer production use

**Problem:** Before a release, you want to `<validation / cleanup / export goal>`.

**Flow:**
1. Run the tool on the target assets
2. Review warnings or preview output
3. Apply the confirmed changes

## Notes

- Supported Unity versions: `<version range>`
- Render pipeline: `<Built-in / URP / HDRP / Any>`
- Limitations: `<short honest list>`

## Support

For product updates and contact details, see `kitforgelabs.com`.
